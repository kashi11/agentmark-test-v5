---
name: image-classifier
object_config:
  model_name: openai/gpt-4o
  schema: 
    type: object
    properties:
      category:
        description: The selected category
        enum: ["cat", "dog", "bird", "fish", "other"]
      confidence: 
        description: Confidence level
        enum: ["high", "medium", "low"]
      reason: 
        type: string
        description:  Brief reasoning for your classification
    required:
      - names
test_settings:
  dataset: classification.jsonl
  evals:
    - llm_classification_judge
  props:
    image_url: "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQNKMjK_3cVpDbePqf_1s1clkD_ZEzSTQEGRw&s"
    categories: "cat, dog, bird, fish, other"
input_schema:
  type: object
  properties:
    image_url:
      type: string
      description: "URL of the image to classify"
    categories:
      type: string
      description: "Comma-separated list of possible categories"
  required:
    - image_url
    - categories
---

<System>
You are an image classification system. Your task is to analyze the provided image and classify it into one of the given categories.
</System>

<User>
Classify this image into one of these categories: {props.categories}

<ImageAttachment image={props.image_url} />
</User>
