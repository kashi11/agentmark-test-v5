---
name: auction-planner
object_config:
  model_name: anthropic/claude-sonnet-4-20250514
  max_calls: 5
  tools: []
  schema:
    type: object
    properties:
      listing_confident:
        type: boolean
        description: "true if the listing page was confidently identified"
      reasoning:
        type: string
        description: Brief explanation of what was found on the page
      load_more:
        type:
          - object
          - "null"
        description: Load More / Show More button if present. null if none.
        properties:
          selector:
            type: string
            description: CSS selector for the Load More button
        required:
          - selector
      paging:
        type:
          - object
          - "null"
        description: Page navigation controls if present. null if none.
        properties:
          selector:
            type:
              - string
              - "null"
            description: CSS selector for the Next page button. null if only numbered links.
          container:
            type:
              - string
              - "null"
            description: CSS selector for the element containing page number links.
          page_urls:
            type: array
            items:
              type: string
            description: "Pagination href URLs found in the HTML; [] if none"
        required:
          - container
          - page_urls
      found_items:
        type: object
        description: Information about the auctions found on the page.
        properties:
          url_pattern:
            type:
              - string
              - "null"
            description: "PATH pattern for auction detail pages using {var} syntax (e.g., /auction/{id})"
          total_count:
            type:
              - integer
              - "null"
            description: Total auction count if visible on page
        required:
          - url_pattern
    required:
      - listing_confident
      - reasoning
      - found_items
    additionalProperties: false
input_schema:
  type: object
  properties:
    base_url:
      type: string
    page_content:
      type: string
      description: Pre-fetched listing page content
  required:
    - base_url
    - page_content
test_settings:
  dataset: datasets/planner_discovery.jsonl
  evals:
    - lot_url_pattern
    - lot_count
    - pagination_detected
---
import PaginationInstructions from './components/pagination-instructions.mdx';

<System>
You are an expert web scraping analyst. You analyze auction house websites
to identify auction sale events and pagination controls. The page content
is provided as cleaned HTML......

<rules>
1. Read the HTML to identify auctions (sale events) and their URL pattern.
2. Find any pagination controls (Load More buttons, page navigation).
3. Return your JSON result with what you found.
</rules>
</System>

<User>
Analyze the auction listing and pagination controls on: {props.base_url}

An "auction" is a SALE EVENT (e.g., "Horlogerie de Collection", "Modern Art Evening Sale").
Look for upcoming auctions on calendar, schedule, or listing pages.
Do NOT confuse with "lots" — a page showing "All Lots (118)" is NOT an auctions listing.

<pre_fetched_content>
{props.page_content}
</pre_fetched_content>

<instructions>
<PaginationInstructions item_type="auction" />
</instructions>

Return your JSON result immediately.
</User>
