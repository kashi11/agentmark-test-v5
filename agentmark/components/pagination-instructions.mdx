Identify how additional {props.item_type} entries are loaded:

1. **Load More button** — report its selector and visible label.
2. **Numbered pagination** — report the URL pattern for page N.
3. **Infinite scroll** — report the XHR endpoint that fetches the next batch, if visible.
4. **None** — all {props.item_type} entries fit on one page.

For each {props.item_type} found, capture its title, date, and detail-page URL.
