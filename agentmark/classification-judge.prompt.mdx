---
name: classification-judge
object_config:
  model_name: gpt-4o
  temperature: 0
  schema:
    type: object
    properties:
      correct:
        type: boolean
        description: "Whether the classification is correct"
      reason:
        type: string
        description: "Brief explanation for the judgment"
    required:
      - correct
      - reason
test_settings:
  props:
    model_output: "The image shows a cat. Classification: cat. Confidence: high."
    expected_classification: "cat"
input_schema:
  type: object
  properties:
    model_output:
      type: string
      description: "The model's classification output"
    expected_classification:
      type: string
      description: "The expected/correct classification"
  required:
    - model_output
    - expected_classification
---

<System>
You are an evaluation judge. Your task is to determine if a classification model's output matches the expected classification.

Be lenient with formatting - the model output may contain additional text, confidence scores, or reasoning. Focus only on whether the core classification matches the expected value.

Consider a classification correct if:
- The expected classification appears in the output
- A synonym or equivalent term is used
- The classification is semantically the same
</System>

<User>
Model output: {props.model_output}

Expected classification: {props.expected_classification}

Does the model's classification match the expected classification?
</User>
