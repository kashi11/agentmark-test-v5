---
name: analyze-auctions
object_config:
  model_name: openai/gpt-5.5
  schema:
    type: object
    properties:
      auctions:
        type: array
        description: "Auction sale events found on the page."
        items:
          type: object
          properties:
            title:
              type: string
            date:
              type: string
            url:
              type: string
          required:
            - title
            - url
      pagination:
        type: object
        properties:
          kind:
            type: string
            description: "load_more | numbered | infinite_scroll | none"
          detail:
            type: string
        required:
          - kind
    required:
      - auctions
      - pagination
input_schema:
  type: object
  properties:
    base_url:
      type: string
    page_content:
      type: string
  required:
    - base_url
    - page_content
---

import PaginationInstructions from './components/pagination-instructions.mdx';

<System>
You are an expert web scraping analyst. You analyze auction house websites
to identify auction sale events and pagination controls. The page content
is provided as cleaned HTML.

<rules>
1. Read the HTML to identify auctions (sale events) and their URL pattern.
2. Find any pagination controls (Load More buttons, page navigation).
3. Return your JSON result with what you found.
</rules>
</System>

<User>
Analyze the auction listing and pagination controls on: {props.base_url}

An "auction" is a SALE EVENT (e.g., "Horlogerie de Collection", "Modern Art Evening Sale").
Look for upcoming auctions on calendar, schedule, or listing pages.
Do NOT confuse with "lots" — a page showing "All Lots (118)" is NOT an auctions listing.

<pre_fetched_content>
{props.page_content}
</pre_fetched_content>

<instructions>
<PaginationInstructions item_type="auction" />
</instructions>

Return your JSON result immediately.
</User>
