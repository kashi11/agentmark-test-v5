# Required to execute prompts with Anthropic Haiku (used by the AI SDK adapter)
ANTHROPIC_API_KEY=

# Optional — only needed to deploy to / forward traces to AgentMark Cloud.
# Get these from `agentmark login` + `agentmark link`, or the Cloud dashboard.
AGENTMARK_API_KEY=
AGENTMARK_APP_ID=
